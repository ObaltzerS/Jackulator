class Main {
    function void main() {
        var CalcLogic calculator;
        var int result;
        let calculator = CalcLogic.new();
        let result = calculator.power(5, 2);
        do Output.printInt(result);
        return;
    }
}
